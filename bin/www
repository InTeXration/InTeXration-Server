#!/usr/bin/env node
var debug = require('debug')('intexration-back');
var app = require('../app');

app.set('port', process.env.PORT || 3030);

var server = app.listen(app.getByRepo('port'), function() {
  debug('Express server listening on port ' + server.address().port);
});